"""Settings screen — connection, default risk limits, and appearance.

Stub: the workspace shell mounts ``render()``; the real cards land in the settings
task. Read-only display of config plus theme swatches.
"""

from __future__ import annotations

import gradio as gr


def render() -> None:
    """Build the Settings screen panel (placeholder)."""
    gr.Markdown("Settings — coming up.")

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: detect-private-key
      - id: debug-statements

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.14.1
    hooks:
      - id: mypy
        additional_dependencies:
          - pydantic>=2.10
          - pydantic-settings>=2.6
          - types-pyyaml>=6.0
          - pandas-stubs>=2.2
          - httpx>=0.28
          - structlog>=24.4
          - typer>=0.15
          - rich>=13.9
          - pynacl>=1.5
          - cryptography>=44.0
          - sqlalchemy>=2.0
        args: [--config-file=pyproject.toml]
        pass_filenames: false
        entry: mypy src/

  - repo: https://github.com/python-poetry/poetry
    rev: 2.0.1
    hooks:
      - id: poetry-check

  # Keep requirements.txt (the Hugging Face Spaces deploy manifest) a generated
  # artifact: regenerate it from poetry.lock whenever the lock or pyproject change.
  # Needs the export plugin once: `poetry self add poetry-plugin-export`.
  # CI also gates this via .github/scripts/check_requirements_sync.py.
  - repo: local
    hooks:
      - id: poetry-export-requirements
        name: Sync requirements.txt from poetry.lock
        entry: poetry export --without-hashes --extras web -f requirements.txt -o requirements.txt
        language: system
        pass_filenames: false
        files: ^(pyproject\.toml|poetry\.lock|requirements\.txt)$

ci:
  autofix_commit_msg: 'style: auto-fix by pre-commit hooks'
  autoupdate_commit_msg: 'chore: update pre-commit hooks'
